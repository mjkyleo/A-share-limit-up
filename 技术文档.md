# 涨停预测工具 v2.5 技术说明文档

> 版本：v2.5（含 2026-08-17 P0 统计根基加固）　　适用平台：Windows 10/11 + Python 3.11　　GUI 框架：PySide6
> 读者：维护者 / 二次开发者。使用说明见 `README.md`，本文档讲**内部实现**。

---

## 目录

1. [总体架构](#1-总体架构)
2. [数据流全链路](#2-数据流全链路)
3. [模块详细说明](#3-模块详细说明)
   - 3.1 loader.py — 数据加载与识别
   - 3.2 calendar_cn.py — A股交易日历
   - 3.3 db.py — SQLite 持久层
   - 3.4 strategies.py — 策略引擎
   - 3.5 backtest.py — 回测引擎
   - 3.6 topdown.py — 自上而下选股方法论引擎（v2.5 新增）
   - 3.7 app.py — GUI 层
   - 3.8 metrics.py — 指标库（P0 新增：成本/净值/夏普/回撤/因子显著性/Spearman/增量IC）
4. [策略算法完整规格](#4-策略算法完整规格)
5. [回测方法论](#5-回测方法论)
6. [数据库 Schema](#6-数据库-schema)
7. [配置参数全表](#7-配置参数全表)
8. [字段字典（71 个原始字段 → 标准字段）](#8-字段字典)
9. [文件识别与命名规范](#9-文件识别与命名规范)
10. [异常处理与鲁棒性设计](#10-异常处理与鲁棒性设计)
11. [已知限制](#11-已知限制)
12. [二次开发指南](#12-二次开发指南)

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        app.py (PySide6 GUI)                  │
│  数据文件Tab │ 预测结果Tab │ 回测评估Tab │ 策略参数Tab │ 日志Tab │
│  + 动态生成的扩展 Tab（每个启用扩展一个）                      │
└──────┬──────────────┬──────────────┬─────────────┬──────────┘
       │              │              │             │
┌──────▼──────┐ ┌─────▼──────┐ ┌─────▼──────┐ ┌────▼─────────┐
│  loader.py  │ │strategies  │ │ backtest   │ │ calendar_cn  │
│  扫描/识别  │ │  策略引擎  │ │  回测引擎  │ │  交易日历    │
│  清洗/合并  │ │  打分/聚合 │ │  IC评估    │ │  时段判断    │
└──────┬──────┘ └─────┬──────┘ └─────┬──────┘ └──────────────┘
       │              │              │
┌──────▼──────────────▼──────────────▼──────────────────────────┐
│                     db.py (SQLite data.db)                     │
│        files 表 │ daily 表 │ predictions 表                    │
└────────────────────────────────────────────────────────────────┘
       ▲                                      │
       │ 唯一外部输入                          │ 扩展框架读写
┌──────┴──────┐                    ┌──────────▼──────────────────┐
│  data/ 文件夹 │              extensions/ 插件层（能力 + 数据源） │
│  ← 通达信导出 │              REGISTRY 自动发现；datasource 注入 │
└─────────────┘              local(loader) / akshare(实时)      │
                                                  └────────────┘
```

**设计原则**：

| 原则 | 体现 |
|---|---|
| 单向依赖 | app → core 模块 → db；core 模块之间不互相 import（calendar 被各方只读引用）；extensions 只依赖 core 与 base，core 不反向依赖 extensions |
| 数据唯一入口 | 所有外部数据只能经 `loader.read_any → clean` 或 `DataSource.fetch`（标准化同口径）进入系统 |
| 状态集中 | 全部持久状态在 `data.db` + `config.json`，GUI 不持有业务状态 |
| 扩展解耦 | 新增能力 = `extensions/` 丢一个文件；新增数据源 = `extensions/datasources/` 丢一个文件；`app.py` 仅一次通用注册循环 |
| 优雅降级 | 缺字段 → 因子跳过且权重自动归一；缺 scipy → 跳过相关性检验；缺文件 → 对应功能禁用并提示；AKShare 取数失败 → 降级为 unknown 而非崩溃 |
| 不可变输入 | 数据文件只读，程序输出（预测/回测）写独立 CSV 和数据库，永不回写源文件 |

---

## 2. 数据流全链路

### 2.1 预测流（每天收盘后）

```
用户投放 data/2026-08-13-close.xlsx
        │
        ▼
loader.scan_folder()          扫描 data/，对每个文件产出 FileInfo
        │                     {日期, 类型(close/open/pool/pred), 能力(可预测/可回测), 说明}
        ▼
db.save_snapshot()            新文件(hash未见过) → 清洗后入库 daily 表
        │
        ▼                        [用户点击「生成预测」]
loader.load_dataset()         全部可预测文件 → clean → 合并
        │                     去重键 (代码6, 数据日期)，优先级: close > pool > open，同级取信息完整度高者
        ▼
strategies.run_prediction()   五策略打分 → P1/P2/P3 三路径聚合 → ×M可买性 ×R风险
        │
        ├──→ 清单A: 明日涨停 TopN        （按综合分排序）
        ├──→ 清单B: 明日涨幅≥4% TopN     （独立 rise4_score 排序）
        └──→ 清单C: 跨日连板候选          （S1≥0.6 约束 + 连板潜力排序）
        │
        ▼
db.save_predictions()         预测落库: run_date=数据日期, target_date=下一交易日(日历计算)
        │
        ▼
GUI 三张表格展示 + 交叉验证 Spearman ρ 显示在信息栏
```

### 2.2 回测流（次日收盘数据到达后）

```
用户投放 data/2026-08-14-close.xlsx → 自动入库(daily 表新增 20260814)
        │
        ▼                        [用户点击「运行回测」]
db.pending_backtest_dates()   找出"有预测且目标日已有数据"的日期
        │
        ▼
backtest.backtest_one()       predictions(target_date) ⋈ daily(target_date) ON 代码6
        │
        ├──→ 整体指标: Top3/5/10 命中率、平均实际涨幅、全样本基准
        ├──→ 单策略IC: 每个策略分 vs 实际涨幅 的 Spearman ρ + p值 + 三档评价
        └──→ 明细表: 每只股票的预测排名/概率 vs 实际涨幅/是否涨停
        │
        ▼
backtest.rolling_ic()         所有可回测日的 IC 均值汇总 → "策略IC滚动汇总"页
                              → 用户据此在「策略参数」页调权重 → 闭环
```

### 2.3 关键时序约束（A股规则）

- 预测目标日 = `calendar_cn.next_trading_day(数据日期)`，周五数据自动指向周一
- 回测配对严格按 `target_date` 等值连接，节假日断档不会错配
- 预测入库用 `INSERT OR REPLACE`，同一天重复生成预测会覆盖旧值，不会产生重复记录

---

## 3. 模块详细说明

### 3.1 loader.py — 数据加载与识别

#### 3.1.1 格式识别 `read_any(path)`

通达信导出的 ".xlsx" 实际是 **GBK 编码的制表符分隔文本**，识别顺序：

```
1. 扩展名 .xlsx/.xls → 先试 pd.read_excel（真Excel）
2. 失败 → 文本分支：编码 ∈ {gbk, gb18030, utf-8-sig, utf-16}
                     × 分隔符 ∈ {\t, ,}，要求解析出 >3 列才接受
3. .csv → 编码 ∈ {utf-8-sig, gbk, gb18030}
4. 全部失败 → raise ValueError（GUI 标记 ❌ 并跳过，不中断扫描）
```

#### 3.1.2 表头模糊匹配 `locate_columns(columns)`

**表头顺序无关**是核心需求。匹配算法：

```
对每个标准字段的候选关键词列表（COLUMN_MAP）：
  1. 表头归一化: 去 [20260813] 类后缀 → 全角括号转半角 → 去空白 → 转小写
  2. 先精确匹配归一化后的表头
  3. 再包含匹配（如 "封流比" 命中 "涨停封流比[20260813]"），
     多个候选时取【最短列名】——避免 "流通市值" 误配到 "流通市值占比XX" 类长列
```

**已知歧义与对策**：

| 歧义 | 对策 |
|---|---|
| `涨幅` vs `5日涨幅`/`开盘涨幅` | 精确匹配优先，`涨幅` 的归一化形式只有精确等于才命中（`5日涨幅` 归一化后是 `5日涨幅` 不等于 `涨幅`）；包含匹配时最短列名规则保护 |
| `换手` vs `竞价换手` | 同上，精确匹配 `换手` 不会命中 `竞价换手` |
| `市盈(动)` vs `TTM市盈率` | 均映射到标准字段 `市盈`，先命中者优先 |
| 同一文件有两个日期后缀不同的同名字段 | 取关键词列表中先命中的那个 |

#### 3.1.3 数值清洗 `cn_num(x)`

| 输入样例 | 输出 | 说明 |
|---|---|---|
| `'+10.19%'` | `10.19` | 通达信带符号百分数 → 百分点数 |
| `'1.65亿'` | `165000000.0` | 中文单位换算 |
| `'3328.58万'` | `33285800.0` | 同上 |
| `'16↑'` / `'101↓'` | `16.0` | 盘口箭头只取数字 |
| `'--'` `''` `'亏损'` `'无'` | `NaN` | 空值语义保留为 NaN，**不填 0**（空值本身是信息，见 v1 分析结论） |
| `'0.0251'`（换手小数口径） | 0.0251→**2.51** | `clean()` 后置修正：若 `换手` 中位数 <1 判定为小数口径，全列 ×100 |

**口径约定**：所有"涨幅类"字段在系统内统一为**百分点数**（10.02 表示 10.02%）；金额类统一为**元**。

#### 3.1.4 文件类型三层判定 `classify_file`

```
输入文件名
  │ 含 排名结果/final/rank/预测/候选/回测/报告 → 'pred'（历史预测，仅回测用）
  │ 含 open/开盘/竞价/早盘/auction            → 'open'  [判定来源: 文件名]
  │ 含 close/收盘/盘后                        → 'close' [判定来源: 文件名]
  │ 无关键词 ↓
classify_by_content()                          [判定来源: 内容特征]
  │ 规则A: median|涨幅 - 开盘涨幅| < 0.5       → 开盘快照（价格未离开盘价）
  │ 规则B: median(换手) ≤ median(竞价换手)×1.5 → 开盘快照（全天只有竞价成交）
  │ 两规则投票，平票视为无法判断
  │ 仍无法判断 ↓
文件修改时间                                   [判定来源: 修改时间]
  │ mtime 小时 <12 → 'open'；≥14 → 'close'；12~14 → 'pool'
```

判定来源写入 FileInfo.说明，在 GUI「数据文件」页可见（如 `时刻判定:内容特征`），方便用户发现命名错误。

#### 3.1.5 合并去重 `load_dataset`

```
全部可预测文件 → clean → concat
排序键: (快照优先级 ↑, 信息完整度 ↓)
  快照优先级: close=0, pool=1, open=2     ← 预测明天永远以收盘数据为准
  信息完整度: 该行非空字段计数
去重键: (代码6, 数据日期) keep='first'
代码6: 从代码中提取6位数字（'SH600110'→'600110'），消除交易所前缀格式差异
```

### 3.2 calendar_cn.py — A股交易日历

**规则实现**：

```python
is_trading_day(d) = (d.weekday() < 5) and (d.strftime('%Y%m%d') not in HOLIDAYS|_EXTRA)
```

- **周末一律闭市**，包括调休上班的周六日（A股特色，与工作日历库不同）
- `HOLIDAYS`：内置 2024-2026 国务院口径闭市表（仅列工作日闭市日）
- `_EXTRA`：由 `config.json → extra_holidays` 注入，应对临时调整（如极端天气休市）

**交易时段状态机** `current_session()`：

| 时间段 | 状态码 | 说明 |
|---|---|---|
| <9:15 | PRE_OPEN | 盘前 |
| 9:15–9:30 | CALL_AUCTION | 集合竞价（9:25 出结果） |
| 9:30–11:30 | MORNING | 上午连续竞价 |
| 11:30–13:00 | LUNCH | 午间休市 |
| 13:00–15:00 | AFTERNOON | 下午连续竞价 |
| ≥15:00 | CLOSED | 已收盘 |
| 非交易日 | OFFDAY | 附下一交易日提示 |

**API**：`is_trading_day / next_trading_day / prev_trading_day / trading_days_between / current_session`，均接受 `date/datetime/None(=今天)`。

### 3.3 db.py — SQLite 持久层

选型理由：Python 内置 `sqlite3`，零安装零配置，单文件可备份；数据量级（每天数百行）远低于 SQLite 舒适区。

**写入策略**：全部 `INSERT OR REPLACE`——幂等，重复导入同一文件/重复生成预测不会产生脏数据。文件去重靠 MD5（前 1MB），内容未变的文件重复扫描不入库。

**daily 表采用 JSON 整行存储**而非宽表：通达信字段经常增减，JSON 保证历史数据不因 schema 演进丢失；读取时 `json.loads` 还原为 DataFrame。代价是无法 SQL 级筛选字段——回测都是全日全量取出，可接受。

**新增方法**：
- `save_predictions(..., detail_keys=...)`：扩展落库时在 `detail` JSON 中额外保留指定因子列（如 `超跌分/承接分`），供回测单因子 IC 拆包。
- `get_code_series(code, end_date, n)`：返回某股票最近 n 个交易日的 `涨幅` 序列（按 date 降序），供抄底扩展的 `exclude_consecutive_down`（排除连续下跌）使用——这是用本地 `data.db` 历史实现的"轻量时序"，无需逐日 K 线。

### 3.4 strategies.py — 策略引擎

详见 [第 4 节](#4-策略算法完整规格)。模块级要点：

- `DEFAULT_CONFIG`：所有参数的唯一事实来源；`load_config` 与用户 `config.json` 深合并（dict 逐键 update），用户缺省键自动回落默认值——**新增参数向后兼容**
- 每个策略函数签名统一为 `fn(row: Series, th: dict) -> float[0,1]`，新增策略只需加函数 + 在 `STRAT_FNS` 注册 + 在权重里给值
- 涨 4% 预测器 `rise4_score` 与涨停策略**完全独立**（面向全样本、无涨停前提），因子权重内联（动量×2）

### 3.5 backtest.py — 回测引擎

详见 [第 5 节](#5-回测方法论)。要点：

- 预测明细的 `detail` JSON 存了每只股票的五个策略分，回测时拆包做**单策略 IC**——这是"分开判断哪些策略表现好"的实现基础
- `STRATEGY_KEYS` 同时包含 v1（A封单强度…）和 v2（S1封单强度…）命名，兼容旧预测文件回测
- IC 计算门槛：有效样本 ≥5 且策略分唯一值 >2，否则跳过（防止常数列算出假相关）
- `LIST_TYPES / DEFAULT_SCORING / HIT_THRESHOLD / STRATEGY_KEYS` 在文件顶部 `try/except` 导入扩展导出的 `EXT_*` 常量并合并——因此抄底/反转/防御清单自动进入下拉框、同口径评分
- **动态因子权重** `dynamic_weight_plan(db, cfg, roll, used)`：基于 `rolling_ic` 结果逐因子给出建议权重（负 IC 可硬 kill 归零），返回计划表供 UI 渲染 / 一键应用
- `regime_style_advice(cfg)` 走 `DataSource` 抽象（默认 akshare）取市场环境，不再硬编码 `ak.stock_zh_a_spot_em()`——本地模式优雅降级为 `unknown`

### 3.6 app.py — GUI 层

- 单窗口 `MainWindow`，固定五个 Tab + **动态生成的扩展 Tab**（每个启用扩展一个），无多线程（数据量小，操作均 <1s；若未来接入全市场 5000 只，扫描/预测需移入 `QThread`）
- 启动流程：`DB连接 → load_config → set_extra_holidays → scan()自动执行 → enabled_extensions(cfg) 构建扩展 Tab`
- 扩展 Tab 由 `_build_extension_tab(ext)` 通用生成：按 `ext.params` **自动渲染参数控件**（float→QDoubleSpinBox / int→QSpinBox / bool→QCheckBox / choice→QComboBox）+ 运行按钮 → `run_extension` 读控件当前值（第三层 `overrides`）→ 调用 `ext.run` → 渲染结果 + 落库
- 回测 Tab「💡 调参建议」子页：`dynamic_weight_plan` 渲染**动态权重计划表**（策略/滚动IC/IC>0天数/当前权重%/建议权重%/动作）+ 两个开关（负 IC 自动归零 / 回测时自动应用）+ 一键应用按钮
- 所有业务调用包 `try/except`：异常写日志 Tab + `QMessageBox` 截断显示（末 500 字符），**GUI 永不因数据异常崩溃**
- `df_to_table` 统一渲染：NaN→空串，浮点保留 3 位有效显示，数值右对齐
   - 模态弹窗仅用于：无数据警告、参数保存成功、严重错误

### 3.7 metrics.py — 指标库（P0 新增 · 纯函数）

被 `backtest.py` / `app.py` 复用的纯函数指标库（无副作用、便于单测）：

- `per_trade_cost_pct(code, bcfg)`：双边成本 = 2×佣金 + 印花税 + 2×滑点；20cm 板用 `slippage_20cm`，其余用 `slippage_main`。主板合计 ≈0.35%，20cm 板 ≈0.55%。
- `net_return_curve`：等权组合净值，返回 `{curve, sharpe, max_dd, win_rate, net_return, mean_daily}`（样本 <2 期时 sharpe/max_dd 为 NaN，避免噪声）。
- `rolling_net_curve`：跨日滚动净值/夏普/回撤（单日 n=1 不计算）。
- `benchmark_curve`：基准净值。
- `daily_ic_series`：每日因子 IC 序列。
- `factor_significance`：单样本 t + Bonferroni 显著性（α 默认 0.05）。
- `factor_correlation`：五因子 Spearman 相关性矩阵。
- `incremental_ic`：正交化后的增量 IC（剥离其他因子后的边际贡献）。

---

## 4. 策略算法完整规格

### 4.1 样本分组（双轨制）

```
今日涨停组（涨幅 ≥ 9.5%）  → 五策略全量打分，先验 15%，概率上限 62%
非涨停组（冲板候选）       → S1/S2/S4 恒为 0，S3 由动能替代，先验 4%，概率上限 25%
```

> 分组阈值由 `is_limit_up(code, 涨幅, cfg)` 按板块自动取（`board_of` 识别主板/20cm/30cm）：
> 主板 9.5% / 创业板·科创板 20cm 19.5% / 30cm 板 29.5%，兼容四舍五入误差（10% 涨停股常显示 9.95%+）。
> 统一涨停判定入口消除了原先散落的 `涨跌幅>=9.5` 硬编码（P0 修复，详见 3.4 与 4.1）。

### 4.2 五策略打分函数（输出均为 [0,1]）

**S1 封单强度**（权重 0.40，回测 IC=+0.598 唯一显著因子）

| 子因子 | 分档 |
|---|---|
| 封流比 = 封单额/流通市值×100 | ≥5%→1.0；≥3%→0.8；≥1%→0.6；<1%→0.2 |
| 封成比 = 封单量/当日成交量 | ≥10→1.0；≥3→0.75；≥1→0.4；<1→0.1 |

取两个子因子均值；仅一个可用时按一个计。字段缺失时由 `封单额_新/流通市值`、`封单量_新/总手` 推算（loader 层完成）。

**S2 封板质量**（权重 0.25，回测 IC=+0.448）

| 子因子 | 分档 |
|---|---|
| 首封时间（距9:30分钟） | ≤0(一字)→1.0；≤5→0.9；≤30→0.7；≤60→0.45；≤270→0.2；尾盘→0.05 |
| 开板次数 | 0 次→1.0（一封到底）；1 次→0.6；2 次→0.3；≥3 次→0.1 |

合成：`0.6×时间分 + 0.4×开板分`；无开板字段只用时间分；无首封时间字段 → **中性 0.5**（不自作聪明）。

**S3 锁仓度**（权重 0.15，v2 重构）

> 关键教训：v1 的"换手 5-15% 黄金区"回测 IC=−0.035 完全失效。实盘显示**缩量锁仓 → 连板延续强**（一字板换手<1% 全部连板）。黄金区理论迁移到 M 可买性因子。

| 子因子 | 分档（越低越强） |
|---|---|
| 换手率 | <3%→1.0；<8%→0.75；<15%→0.5；<25%→0.25；≥25%→0.05 |
| 振幅 | ≤0.5%→1.0；≤3%→0.8；≤7%→0.5；>7%→0.2 |
| 量比 | <0.8→1.0；<1.5→0.7；<3→0.4；≥3→0.2 |

**S4 资金**（权重 0.05，回测 IC=−0.207 呈弱反向 → 降权观察而非删除）

主力净量 / 增仓占比 / 机构动向三子因子分档取均值。保留原因：单日样本不足以下"反向"结论，降权后若滚动 IC 持续为负可在参数页归零。

**S5 股性结构**（权重 0.15）

| 子因子 | 分档 |
|---|---|
| 流通市值 | <30亿→1.0；<50→0.85；<80→0.65；<150→0.4；<300→0.2；≥300→0.05 |
| 年涨停次数（股性） | ≥10→1.0；≥5→0.75；≥2→0.5；<2→0.25 |
| 20日涨幅（位置） | <20%→1.0；<40→0.7；<60→0.4；≥60→0.1（高位减分） |

### 4.3 三路径聚合与交叉验证

```
P1 加权得分  = Σ(策略分 × 权重)                    保留量级信息
P2 Borda名次 = Σ(策略组内名次 × 权重) / 组大小      只看相对排序，抗异常值
               （涨停组/冲板组各自内部排名，避免跨组量级不可比）
P3 先验概率  = 基准先验 + P1 × 修正系数             输出可解释为概率
               涨停组: min(0.15 + P1×0.35, 0.62)
               冲板组: min(0.04 + P1×0.16, 0.25)

综合分 = mean(P1_z, P2_z, P3_z) × M可买性 × R风险系数
         （_z 为 min-max 归一化）
```

**交叉验证**：涨停组内计算 P1×P2、P1×P3、P2×P3 两两 Spearman ρ。
注意 P3 是 P1 的单调变换（ρ≈1 是必然），**真正独立的检验是 P2 vs P1/P3**；
ρ>0.9 说明排名对聚合方法稳健。实证（08-13，n=52）：ρ=0.987。

### 4.4 M 可买性乘子（0~1）

解决"一字板买不进"：涨停概率再高，买不进期望就是零。

```
b = 1.0
× 封板用时: 一字×0.10 | ≤30秒×0.35 | ≤2分×0.55 | ≤5分×0.75 | 更晚×0.9
× 封成比:   >100×0.25 | >30×0.5 | >15×0.75 | 其余×1.0   （封单越巨，次日一字开概率越高）
× 开盘涨幅: 今日高开>8% ×0.8                            （惯性高开压缩买入窗口）
× 换手率:   <2% ×0.6                                   （惜售，筹码难买）
非涨停股: b = 0.9
```

### 4.5 R 风险惩罚乘子（0~1）

| 条件 | 乘子 | 依据 |
|---|---|---|
| 亏损股（市盈='亏损'） | ×0.85 | 基本面风险 |
| 开板次数 ≥3 | ×0.60 | 反复开板=巨大分歧 |
| 换手 >25% | ×0.70 | 分歧过大 |
| 20日涨幅 >60% | ×0.60 | 高位接力风险 |
| 名称含 ST | ×0.50 | 5% 涨跌幅限制+退市风险 |

### 4.6 涨幅≥4% 预测器 `rise4_score`

面向**全部股票**（无涨停前提），目标宽松（+4% 即命中）：

| 因子 | 权重 | 最优区间 |
|---|---|---|
| 今日涨幅（动量） | ×2 | 3%~8% 最佳；≥9.5%(涨停) 0.8；1~3% 0.6；<0 0.1 |
| 量比 | ×1 | 1.5~3 最佳 |
| 资金流向 | ×1 | >0 即满分 |
| 主力净量 | ×1 | ≥1 满分 |
| 开盘涨幅 | ×1 | 1%~5% 竞价强势 |
| 5日涨幅 | ×1 | 0~20% 未过热 |
| 流通市值 | ×1 | <80亿 |

概率换算：`min(0.10 + 评分×0.35, 0.55) × R风险系数`。

### 4.7 跨日连板候选

```
候选池 = 今日涨停 且 S1封单强度 ≥ 0.6    （无人满足时放宽为全部涨停股并标注提示）
连板潜力 = 0.5×S1 + 0.3×S2 + 0.2×S3    （故意不含 S4资金/S5结构）
取 Top monday_n（默认8）
```

逻辑：周一能涨停的前提是中间交易日先连板，只有**封单最强+封板最稳+锁仓最死**的票才有连续溢价；资金面当日回测为反向因子，不纳入。

---

## 5. 回测方法论

### 5.1 配对逻辑

```
predictions.target_date == daily.date   （严格等值，交易日历保证不错配节假日）
连接键: 代码6（6位数字，消除 SH/SZ 前缀格式差异）
```

### 5.2 评估指标

**整体层**：
- Top3/5/10 涨停命中率、平均实际涨幅
- 全样本基准（同日池内全部股票平均涨幅）——判断超额来自模型还是市场

**策略层（核心功能"分开判断策略表现"）**：
- 单策略 IC = Spearman(策略分, 次日实际涨幅)，附 p 值
- 三档评价：`ρ>0.4 且 p<0.1 → ✅有效`；`ρ>0 → ⚠️弱`；`ρ≤0 → ❌无效/反向`
- 计算门槛：有效样本 ≥5 且策略分唯一值 >2（防常数列假相关）
- 兼容性：同时识别 v1 命名（A封单强度…）与 v2 命名（S1封单强度…）

**滚动层**：
- 全部可回测日的 IC 均值 + IC>0 天数占比
- 评价：`均值>0.3 → ✅稳定有效`；`0~0.3 → ⚠️不稳定`；`≤0 → ❌建议下调权重`
- **这是调参依据**：积累 ≥10 个交易日后，把权重调成与滚动 IC 大致成比例

### 5.3 已验证的回测结论（08-13 实盘，n=16 一进二组）

| 策略 | IC(ρ) | p | 处置 |
|---|---|---|---|
| 封单强度 | +0.598 | 0.014 | 权重升至 0.40 |
| 封板时间 | +0.448 | 0.082 | 权重 0.25，并入开板次数 |
| 量能换手(v1) | −0.035 | 0.899 | 重构为锁仓度 |
| 市值 | +0.071 | 0.794 | 降权并入股性结构 |
| 资金 | −0.207 | 0.442 | 降权至 0.05 观察 |

当日一进二组实际连板率 31%（基准 15%），v1 模型 Top3 命中 1/3——**有效因子权重不足、无效因子稀释信号**是 v1 的主要问题，v2 权重由此而来。

### 5.4 动态因子权重（v2.3 新增）

针对"所有因子 IC 同时为负"的系统性失效，引入**基于滚动 IC 的逐因子权重计划** `dynamic_weight_plan`：

```
输入: rolling_ic 结果(各策略 平均IC, IC>0天数) + used(可回测天数) + cfg
规则:
  对每个策略 k:
    ic = 平均IC
    if kill_neg 且 (ic 缺失 或 ic ≤ 0): 建议权重 = 0%     ← 硬 kill，真正解决"团灭"
    else:                           pos_k = max(ic, 0)
  若所有 pos 之和 ≤ 0:               建议权重 = 全 0（空仓信号）
  否则:  归一化 + 2% 地板:
    sug_k = floor + (1 - floor*len) * pos_k / Σpos     ← 五因子权重和恒为 100%
开关(min_days 默认 3): used < min_days 时不输出数值建议
```

作用链：回测 Tab「💡 调参建议」渲染计划表 → 勾选「负IC自动归零」+「回测时自动应用」→ 每次回测自动写 `config.weights` 并刷新参数页。**注意**：归零只是"不硬扛失效因子"，不凭空造 alpha；它配合抄底/反转/防御三件套（risk-off 时主攻）才是完整解法。

### 5.5 市场环境识别（market_regime，v2.3）

`regime_style_advice(cfg)` 经 `DataSource`（默认 akshare）取得全市场快照 + 活跃度，输出四态之一：

| 状态 | 判定（示例阈值） | 建议主攻清单 |
|---|---|---|
| risk_on | 指数 20日涨幅 >0、涨跌家数比高、成交温和放大 | 涨停TopN / 连板 / 尾盘 |
| risk_off | 指数 20日涨幅 <0、涨跌比低、缩量 | 抄底 / 短线反转 / 防御 |
| panic | 涨跌比极低（如 <0.5）、大面积普跌 | 防御 + 空仓观察 |
| neutral / 震荡 | 波动率低、成交萎缩 | 流动性反弹 / 底部吸筹 |

取数失败（无网/被限速）时降级为 `unknown`，不阻断回测。

---

## 6. 数据库 Schema

`data.db`（SQLite，程序目录下，**删除会丢失回测历史**）：

```sql
files(                                -- 已入库文件登记
  hash TEXT PRIMARY KEY,              -- MD5(前1MB)，内容级去重
  path TEXT, date TEXT,               -- 数据日期 YYYYMMDD
  kind TEXT,                          -- close/open/pool
  rows INT, imported_at TEXT)

daily(                                -- 每日每股票清洗后指标
  date TEXT, code TEXT,               -- code=6位数字
  name TEXT, json TEXT,               -- 整行JSON（字段演进不丢历史）
  PRIMARY KEY(date, code))

predictions(                          -- 预测记录
  run_date TEXT,                      -- 数据日期（哪天收盘后跑的）
  target_date TEXT,                   -- 预测目标交易日
  list_type TEXT,                     -- '涨停TopN'/'涨幅4%'/'连板候选'/'尾盘选股'
                                      -- / '抄底清单'/'短线反转'/'防御低波'（扩展自动并入）
  rank INT, code TEXT, name TEXT,
  prob REAL, score REAL,
  detail TEXT,                        -- 五策略分+乘子 JSON（回测拆包用）；
                                      -- 扩展清单额外保留 detail_keys 指定的因子列
  PRIMARY KEY(run_date, list_type, code))
```

常用排查 SQL：

```sql
SELECT date, COUNT(*) FROM daily GROUP BY date;            -- 每天多少只
SELECT target_date, list_type, COUNT(*) FROM predictions
  GROUP BY target_date, list_type;                          -- 每天的预测
SELECT * FROM files ORDER BY imported_at DESC;              -- 导入记录
```

---

## 7. 配置参数全表

`config.json`（GUI 参数页可视化编辑，也可手改；缺失键自动回落默认值）：

| 键 | 默认 | 含义 |
|---|---|---|
| `weights.S1封单强度` | 0.40 | 五策略权重（自动归一，无需凑整为1） |
| `weights.S2封板质量` | 0.25 | 同上 |
| `weights.S3锁仓度` | 0.15 | 同上 |
| `weights.S4资金` | 0.05 | 同上（回测反向，观察中） |
| `weights.S5股性结构` | 0.15 | 同上 |
| `base_limit_up` | 0.15 | 一进二基准先验（文献 12%~19%） |
| `base_rush` | 0.04 | 冲板基准先验 |
| `base_rise4` | 0.10 | 次日涨≥4%基准（经验值，可回测校准） |
| `thresholds.涨停判定涨幅` | 9.5 | 主板涨停识别线（%），由 `is_limit_up` 按 `board_of` 自动选用 |
| `thresholds.涨停判定涨幅_20cm` | 19.5 | 创业板/科创板 20cm 板涨停识别线（%，自动选用） |
| `thresholds.涨停判定涨幅_30cm` | 29.5 | 30cm 板涨停识别线（%） |
| `thresholds.封流比_强/_极强` | 3.0/5.0 | S1 分档 |
| `thresholds.封成比_强/_极强` | 3.0/10.0 | S1 分档 |
| `thresholds.锁仓换手_极紧/紧/松` | 3/8/15 | S3 分档（%） |
| `thresholds.小市值上限` | 80 | S5 市值档（亿元） |
| `thresholds.高位20日涨幅` | 60 | 位置风险线（%） |
| `thresholds.开板次数_风险` | 3 | R 风险触发 |
| `top_n / rise4_n / monday_n` | 20/20/8 | 三张清单长度 |
| `extra_holidays` | [] | 额外闭市日 `['20261225', ...]` |
| `extensions.<key>.enabled` | true | 是否启用该扩展（启用才生成 Tab + 参与回测） |
| `extensions.<key>.datasource` | 'local'/'akshare' | 该扩展的数据源 key（框架据此注入数据） |
| `extensions.<key>.params` | {} | 覆盖扩展 `Param.default` 的参数（运行 / 调参皆以此为准） |
| `dynamic_weights.enabled` | true | 回测时是否计算并应用动态因子权重 |
| `dynamic_weights.method` | 'shrinkage' | 调权算法：shrinkage（收缩至地板，非硬 kill） |
| `dynamic_weights.ema_window` | 10 | 每日 IC 的 EMA 平滑窗口（交易日） |
| `dynamic_weights.shrinkage_lambda` | 0.3 | 向地板收缩系数 λ（权重=(1-λ)·max(EMA_IC,0)+地板） |
| `dynamic_weights.significance_alpha` | 0.05 | Bonferroni 显著性 α（按因子数校正） |
| `dynamic_weights.auto_kill_negative` | true | 语义已改为"仅对显著因子调权"开关（不再是字面归零） |
| `dynamic_weights.auto_apply` | false | 回测时自动把建议权重写入 config（否则仅展示） |
| `dynamic_weights.floor` | 0.02 | 不显著因子的权重地板（2%，收缩至此而非 0） |
| `dynamic_weights.min_days` | 3 | 可回测天数达此值才输出数值建议 |
| `timing.method` | 'synthetic' | 择时模式：合成（上证+中证1000+创业板指+涨跌家数宽度，四分量均值仓位） |
| `timing.enabled` | true | 是否启用择时 |
| `timing.breadth_fallback` | true | 取不到指数时用涨跌家数市场宽度兜底 |
| `backtest_cost.commission` | 0.00025 | 单边佣金率 |
| `backtest_cost.stamp_tax` | 0.001 | 印花税（卖出单边） |
| `backtest_cost.slippage_main` | 0.001 | 主板滑点（双边计入） |
| `backtest_cost.slippage_20cm` | 0.002 | 20cm 板滑点（双边计入，更高） |
| `backtest_cost.caliber` | 'close' | 成本计算口径（以收盘价计） |

---

## 8. 字段字典

通达信原始字段 → 标准字段映射（覆盖率基于 08-13-close.xlsx 65 只实测）：

### 8.1 核心行情（100% 覆盖）

| 标准字段 | 原始列名候选 | 口径 | 用途 |
|---|---|---|---|
| 涨幅 | 涨幅/涨跌幅 | 百分点（10.02=10.02%） | 分组+动量 |
| 开盘/昨收/最高/最低/现价 | 同名 | 元 | 衍生计算 |
| 开盘涨幅 | 开盘涨幅 | 百分点 | 竞价强弱、可买性 |
| 量比 | 量比 | 倍 | S3、rise4 |
| 换手 | 换手/换手率 | 百分点（自动识别小数口径×100） | S3、R |
| 振幅 | 振幅 | 百分点 | S2、S3 |
| 竞价换手 | 竞价换手 | 百分点 | 开盘快照识别、rise4 |

### 8.2 封板质量（仅涨停股有值，空值即"未封板"信息）

| 标准字段 | 原始列名 | 用途 |
|---|---|---|
| 封流比 | 涨停封流比[yyyymmdd] | S1（缺失时由 封单额/流通市值 推算） |
| 封成比 | 涨停封成比[yyyymmdd] | S1（缺失时由 封单量/总手 推算） |
| 封单额/封单量 | 封单额_新/封单量_新 | S1 推算源 |
| 首封时间 | 首次涨停时间[yyyymmdd] | S2、M |
| 开板次数 | 涨停开板次数[yyyymmdd] | S2、R |
| 年涨停数 | 涨停次数[起-止] | S5 股性 |

### 8.3 资金与结构

| 标准字段 | 原始列名 | 用途 |
|---|---|---|
| 主力净量 | 主力净量 | S4、rise4 |
| 增仓占比/机构动向/主力买入/资金流向 | 主力增仓占比/机构动向/主力买入金额/主力资金流向 | S4、rise4 |
| 流通市值 | 流通市值 | S1推算、S5、rise4 |
| 内外比/笔数/总手 | 同名 | S1推算、预留 |
| 5/10/20日涨幅 | 同名 | S5 位置、rise4 过热 |
| 市盈/市净率/净利增长/散户数量/上市日期 | TTM市盈率含'亏损'文本等 | R 风险、预留 |
| 竞价评级/异动类型 | 同名（覆盖31%~38%） | 预留事件标签 |

### 8.4 弃用字段（及原因）

备注（0%覆盖）、自选价格/自选收益/自选时间（个人操作记录）、现手（瞬时噪声）、贡献度（对指数非个股）、20240924至今（固定口径无信息量）、买价/卖价/买量/卖量（收盘瞬时值，涨停股卖价恒为空）。

---

## 9. 文件识别与命名规范

**投放位置**：`程序目录/data/`（不存在自动创建）。

**建议命名**（`YYYY-MM-DD` 与 `MM-DD` 均可）：
```
2026-08-13-close.xlsx   收盘（每天必有）
2026-08-13-open.xlsx    开盘/竞价（可选）
2026-08-13-自选.xlsx    股票池
```

**日期提取优先级**：列名后缀 `[20260813]` → 文件名完整日期 → 文件名 `MM-DD`（补当年） → 文件修改时间。

**类型三层判定**：文件名关键词 → 内容特征（涨幅≈开盘涨幅 且 换手≈竞价换手 → open） → 修改时间（<12点 open，≥14点 close）。判定来源显示在 GUI。

**输出文件保护**：文件名含 `排名/预测/周一/回测/报告/final/rank/候选` 的一律不作为数据导入，防止程序输出被误当输入形成循环。

---

## 10. 异常处理与鲁棒性设计

| 场景 | 行为 |
|---|---|
| 文件格式无法识别 | 扫描页标 ❌ + 原因，跳过不中断 |
| 表头缺列 | 对应因子跳过，策略分按可用子因子均值，权重自动归一 |
| 整列空值（如自选池无首封时间） | 该策略给中性分 0.5（S2），其余策略不受影响 |
| 换手口径不一致（0.08 vs 8%） | 中位数<1 判定小数口径自动 ×100 |
| 重复导入同一文件 | MD5 去重，不重复入库 |
| 重复生成预测 | INSERT OR REPLACE 覆盖，无重复行 |
| 未装 scipy | 跳过相关性检验并提示，主功能不受影响 |
| 回测匹配样本 <3 | 提示样本不足，不输出误导性指标 |
| 任意业务异常 | 日志 Tab 记录完整堆栈，弹窗截断提示，GUI 不崩溃 |
| config.json 损坏/缺键 | 回落默认配置 |
| 创业板/科创板 20cm 股 | 涨停判定线 9.5% 可识别其涨停态；非涨停态高涨幅会高估冲板分（见限制） |

**已通过的测试**：乱序表头+改日期后缀+删列（排名不变）、伪 xlsx/CSV/真 xlsx 三格式、伪造开盘快照内容识别、GUI 离屏冒烟（建窗/扫描/预测/回测/保存参数全链路）。

---

## 11. 已知限制

1. **单日回测样本**：当前策略 IC 仅基于 1 天（n=16），S4 资金"反向"结论需 ≥10 天滚动 IC 确认后再处置
2. **20cm 板块**：创业板/科创板涨停为 20%，已由 `is_limit_up`/`board_of` 按代码前缀（300/301/688）自动取 19.5% 判定线（P0 已落地，非"二次开发预留"）；其 +12% 的普通上涨在 20cm 板口径下仍按非涨停处理，动量分按板块口径计算
3. **自选池缺首封时间**：S2 对池内股票只能给中性 0.5，区分度受限；建议通达信导出时勾选"首次涨停时间"列
4. **无板块/题材数据**：连板高度受板块梯队影响很大，当前模型不含此维度
5. **概率口径**：输出概率锚定文献基准+单日校准，应理解为**相对排序**而非绝对频率
6. **GUI 单线程**：全市场 5000 只级别数据需把扫描/预测移入 QThread（当前自选池规模无感）
7. **AKShare 数据源**：免费无需 key，但偶发反爬/限速、需联网；取数失败时对 `market_regime` 降级为 unknown，对其它实时扩展需自行重试（已内置重试退避）
8. **local 数据源扫描面有限**：导入文件多为涨停/强势/自选精选子集（约 300 只），抄底/反转/防御在 `local` 模式下只能扫到这些，**全市场超跌扫描必须用 `akshare` 数据源**
9. **动态权重仅作用于五策略**：抄底/反转/防御是独立扩展逻辑，不受 `dynamic_weights` 调度，其启用与否由 `market_regime` 建议 + 用户手动切换决定
10. **净值/夏普/回撤口径**：单日组合 n=1 时样本不足，净值曲线与夏普/回撤由 `core/metrics.py` 在**跨日层**计算，单日不输出噪声指标
11. **TopN「排序分」非真实概率**：GUI 的「预估涨停概率%」列已改名为「排序分（非真实概率）」并附脚注——它只是五因子加权排序分，不是校准概率，仅用于排序

---

## 12. 二次开发指南

### 新增一个策略因子（示例：加入"竞价强度"）

```python
# strategies.py
def s6_auction(r, th):
    if not r['今日涨停']:
        return 0.0
    if pd.isna(r.get('竞价换手')):
        return 0.5
    return 1.0 if r['竞价换手'] >= 1 else 0.6 if r['竞价换手'] >= 0.3 else 0.3

STRAT_FNS['S6竞价强度'] = s6_auction
DEFAULT_CONFIG['weights']['S6竞价强度'] = 0.10   # 旧 config.json 无此键会自动回落
```

→ 重启程序即生效；回测模块会自动把 S6 纳入 IC 评估（把键名加进 `backtest.STRATEGY_KEYS`）。

### 接入全市场数据

loader 无需改动（表头匹配与文件大小无关）；建议：①`load_dataset` 加分页或只保留涨幅>0 股票；②GUI 操作移入 `QThread` + 进度条；③`data.db` 的 daily 表加 `CREATE INDEX ON daily(code)`。

### 打包为 exe（免 Python 环境分发）

```cmd
pip install pyinstaller
pyinstaller -F -w app.py -n 涨停预测工具
```
生成单文件 exe；注意 `data/`、`config.json`、`data.db` 会创建在 **exe 所在目录**（`BASE_DIR` 用 `__file__`，PyInstaller 下需改为 `sys.executable` 目录，见 loader 注释）。

---

*文档版本 v2.3 · 与代码同步更新 · 疑问先查「📜 日志」Tab 的堆栈记录*

---

## 13. 扩展框架（extensions）—— 灵活集成新能力 / 实时接口

v2.2 引入的插件式扩展层。**新增一项能力（如抄底反弹）= 只往 `extensions/` 丢一个 Python 文件**，无需改动 `app.py` 核心、`strategies.py`、`backtest.py`。新增一个实时数据源 = 往 `extensions/datasources/` 丢一个文件。

### 13.1 目录

```
extensions/
  __init__.py        # 导入即自动发现 Extension 子类 → REGISTRY；并导出给 backtest 合并的清单类型
  base.py            # Extension / Param / DataSource 抽象基类（能力的"所有要求"都在这里表面）
  bottom_fishing.py  # 抄底反弹扩展（首个能力示例）
  short_reversal.py  # 短线反转扩展（与涨停接力正交）
  defensive.py       # 防御低波扩展（系统性下行时的"盾"）
  neural_predict.py  # 神经网络预测（独立 NN 引擎，训练/推理自举于 AKShare 历史）
  topdown_pick.py    # 自上而下选股扩展（择时→板块主线→龙头，v2.5 新增）
  market_regime.py   # 市场环境识别（输出 risk_on/off/panic/neutral + 建议主攻清单）
  datasources/
    __init__.py      # 自动发现 DataSource 子类 → DATA_SOURCES
    local.py         # LocalFolderDataSource（复用 loader，无网络）
    akshare.py       # AKShareDataSource（实时全市场快照 + 活跃度，免费）
```

### 13.2 写一个能力扩展（抄底即此范式）

继承 `extensions.base.Extension`，填属性 + 实现 `run(df, cfg, ctx)`：

| 属性 | 作用 |
|---|---|
| `key` / `name` / `description` | 身份与说明（GUI Tab 标题取自 name） |
| `params = [Param(...)]` | 可配置参数；GUI **自动渲染**对应控件，无需手写界面 |
| `datasource` | 数据源 key（框架据此注入数据，扩展不关心来源） |
| `list_type` / `scoring` / `hit_threshold` / `detail_keys` | 回测注册：清单名 + 打分规则 + 达成阈值 + 入 detail 的因子列（供单因子 IC） |
| `run(df, cfg, ctx) -> {'主表', 'note', 'tables'}` | 核心逻辑；返回 DataFrame 即被渲染、被落库、被回测 |

`Param(name, label, kind, default, min, max, choices, step, help)`，`kind ∈ {float,int,bool,choice,str}`，GUI 据此渲染对应控件（`str` → 单行文本框，`choice` → 下拉框，其余 → 数值框/勾选框）。

### 13.3 写一个数据源（接实时行情）

继承 `extensions.base.DataSource`，实现 `fetch(ctx) -> (df, info)`，把任意来源（akshare / 东方财富 / 通达信实时）的数据清洗成与 `loader.clean` 同口径的标准列（含 `代码6/名称/涨幅/20日涨幅/量比/换手/主力净量/资金流向/最高/最低/现价/亏损股/数据日期...`）。在 `config.extensions.<能力>.datasource` 指向它的 `key` 即可切换——**能力逻辑零改动**。

### 13.4 启用与配置

`config.json` 的 `extensions` 节控制每个能力的启用与参数覆盖（运行时从 `Param.default` 合并）：

```json
"extensions": {
  "bottom_fishing": { "enabled": true,
    "params": { "min_drop": 7.0, "max_drop": 9.5, "min_20d_change": 0.0,
                "require_support": true, "bottom_n": 20, ... } }
}
```

### 13.5 GUI / 回测联动

- `app.py` 启动时 `enabled_extensions(cfg)` 遍历启用的扩展，**动态生成 Tab**（自动参数控件 + 运行按钮 + 结果表格）；运行后结果经 `db.save_predictions(list_type=扩展.key, detail_keys=扩展.detail_keys)` 落库。
- `backtest.py` 顶部 `try/except` 导入扩展导出的 `EXT_LIST_TYPES/EXT_SCORING/EXT_HIT/EXT_IC_KEYS`，自动并入 `LIST_TYPES/DEFAULT_SCORING/HIT_THRESHOLD/STRATEGY_KEYS`。因此**抄底清单在「回测评估」下拉框中直接可选**，打分、达成率、单因子 IC 同口径生效。

### 13.6 抄底反弹扩展逻辑要点（bottom_fishing）

三层筛选交集：① 今日急跌（跌幅 ∈ `[min_drop, max_drop]`，默认 7%~9.5%，方向/区间可调）② 非大下跌趋势（`20日涨幅≥min_20d_change` 且 `5日涨幅≥min_5d_change`；可选 `require_support` 要收盘远离最低=有承接；可选 `exclude_consecutive_down` 用 `data.db` 历史排除连跌）③ 六因子打分（超跌度+量能健康+资金未逃+承接力+位置+股性）×风险系数 → 排序 → 预估反弹概率。`exclude_consecutive_down` 依赖 `db.get_code_series(code)`。

### 13.7 其余三个正交扩展

| 扩展 key | name | 风格 | 核心筛选 / 打分 | 截面 |
|---|---|---|---|---|
| `short_reversal` | 短线反转 | 与涨停接力**正交** | 跌幅 ∈ [max_drop, min_drop]、振幅够大、下影线比≥阈值（盘中承接）、放量（量比≥阈值）、非长期下跌（60日涨幅闸门）、流通市值适中 | risk-off 主攻 |
| `defensive` | 防御低波 | 对立（盾） | 低波动（振幅/量比小）+ 估值合理（PE/PB 正向）+ 盈利稳定（非亏损）+ 中期趋势未坏（20日涨幅>0）+ 市值不宜过小 | 系统性下行 |
| `market_regime` | 市场环境识别 | 元策略（不参与选股） | 经 DataSource 取全市场涨跌家数/指数走势/成交额 → risk_on/off/panic/neutral；无网降级 unknown | 每日必跑，决定其它清单优先级 |

三者的 `run()` 对 AKShare 模式缺失列（缺 5/10/20日涨幅、主力净量、资金流向）均做了**优雅退化**（用 60日涨幅代理趋势、资金因子给中性 0.5），因此本地 `local` 与实时 `akshare` 两种数据源都能跑通。回测注册（`list_type`/`scoring`/`hit_threshold`/`detail_keys`）由各自 `Extension` 属性声明，`backtest.py` 顶部自动合并，无需手改。

### 13.8 神经网络预测扩展（neural_predict，v2.4 新增 · 独立能力）

与其它四个"规则/阈值"扩展不同，这是一个**数据驱动**的独立预测视角，刻意与五因子线性体系正交。设计目标是"功能独立"：

- **存储独立**：模型与历史单独存 `nn_model.db`（`NeuralEngine`），**不污染主 `data.db`**；表 `nn_history`（每日 OHLCV 缓存）+ `nn_model`（后端/缩放器/模型 blob/meta/样本数）。
- **训练自举**：本地 `data/` 仅 2 天快照不足以训练，故首次训练必须外援 **AKShare 历史日 K 线**（`stock_zh_a_hist`）。`bootstrap(codes, lookback_days)` 对股票池逐股拉历史，单股 3 次重试 + 退避应对东财限速；`build_dataset(min_hist)` 用**滑动窗口**逐日构造样本：`t` 处特征 → `t+1` 处涨跌幅标签。
- **特征独立**：12 维纯量价特征（见 `compute_features`：`ret_1/5/10/20/60`、`vol_20`、`vol_ratio`、`gap`、`range_pos`、`amt_z`、`up_ratio`、`ma_ratio`），**不依赖封流比等涨停细节字段**——这是它与五因子解耦的关键。
- **双后端**：默认 `sklearn` `MLPRegressor`（隐藏层 `hidden` 可调、早停）；可选 `torch`（PyTorch 深网，`weights_only` 安全加载）。`train(X, y, cfg)` 统一入口，`predict_features(X_df)` 统一推理。
- **推理**：`run(df, cfg, ctx)` 为快照每只股票用缓存历史 + 当日快照构造最新 bar → 算特征 → 预测次日涨幅 → sigmoid 映射为「上涨概率%」与「NN评分」；不在股票池的股票可按 `fetch_missing` 临时补拉（慢，默认关）。
- **GUI 联动**：`Extension.supports_training = True` → 框架自动渲染 **「🧠 训练/更新模型」** 按钮（仅 `datasource=='akshare'` 时可用），点击后在 `TrainWorker` 后台线程训练，不卡 UI；`auto_train` 开启时首次「运行」自动训练。

> 独立性体现在三方面：① 数据存储独立（`nn_model.db`）；② 特征来源独立（通用量价，不碰封板字段）；③ 模型逻辑独立（非线性 NN，与线性五因子正交）。两者由回测 IC / 动态权重客观裁决"今天信谁"。

### 13.9 自上而下选股扩展（topdown_pick，v2.5 新增 · 选股指导思想）

把用户确立的选股原则「**用上证指数择时 → 行业板块找主线 → 主线龙头找个股**」落成
可量化、可回测的引擎 `core/topdown.py`，并封装为扩展 `topdown_pick.py`（与抄底/反转/防御/神经网络并列）。
它回答的不是"哪只明天会涨停"，而是"**现在该不该出手、往哪个行业、选板块里哪只龙头**"——管节奏与方向，与前三者正交。

引擎四层（见 `core/topdown.py`）：

- **L1 择时 `market_timing`**：优先 `fetch_sh_index` 取上证指数 `sh000001` 日线 → `timing_from_index` 用 20/60 日线位置、近 5/20 日收益、多头/空头排列打分 → 输出 `市道标签 + 仓位系数(0~1)`；取不到指数时 `timing_from_breadth` 用自选池**市场宽度**代理（上涨比例/涨幅中位数/涨停数），保证离线可用。
- **L2 板块主线 `sector_strength`**：按 `所属行业` 聚合，板块强度 = `0.35·平均涨幅 + 0.25·平均20日 + 0.20·涨停浓度 + 0.20·主力净量`，跨板块 z 化到 0~100；`main_lines(top_n)` 取强度 TopN 即「主线」。
- **L3 龙头个股 `leader_score`**：主线板块内按 `0.4·动量 + 0.2·主力净量 + 0.15·流通市值(龙头偏大) + 0.15·封板质量 + 0.1·板块强度` 打分；**非主线个股综合得分乘 `(1 - gate_non_main)` 强降权**（默认 70%），确保榜单由主线龙头主导。
- **L4 综合 `topdown_rank`**：`综合得分 = (0.6·龙头评分 + 0.4·板块强度) · 择时系数 · (非主线降权)`，返回 `(timing_dict, sector_df, ranked_df)`。

`run(df, cfg, ctx)` 取 `normalize_market_cols(df)`，调用 `topdown_rank`，保留 TopN 并附「板块强度」子表；`datasource='local'`（作用于已加载自选池），择时优先 AKShare 上证指数、回退宽度代理。`config.extensions.topdown_pick.params` 可调：`top_n_sectors / min_sector_count / timing_method(auto|index|breadth) / weight_sector / weight_leader / gate_non_main / top_n`。回测以 `list_type='自上而下'`、`hit_threshold=2.0` 注册，与五因子同口径评分。

> 实战含义：该扩展是**总开关**。择时=进攻才重仓顺势；=防御则降到轻仓甚至空仓，无论个股多看好。它把"自上而下"从理念变成了可在回测里与其他能力并列裁决的量化清单。

### 13.10 AKShare 实时数据源（akshare.py）

继承 `extensions.base.DataSource`，`fetch(ctx)` 流程：

```
1. stock_zh_a_spot_em()            全市场 ~5000 只快照（含 代码/名称/涨幅/最高最低/量比/换手/市值/PE...）
2. stock_market_activity_legu()   市场活跃度（涨跌家数、涨停/跌停家数等）→ info['market_activity']
3. 清洗为 loader.clean 同口径标准列（代码6 取 6 位数字、涨幅统一百分点、金额统一元）
4. 重试退避（被反爬/限速时）+ 浏览器 UA；失败抛异常由调用方降级
```

- **接入零改动**：在 `config.extensions.<能力>.datasource` 把 `'local'` 改为 `'akshare'` 即可让该能力改走实时全市场数据——能力 `run()` 逻辑一字不改。
- **能力差异**：`local` 覆盖约 300 只精选子集（含封板细节，涨停接力必需）；`akshare` 覆盖全市场但**无封板字段**（故封单/封板/锁仓策略仍依赖 `local` 导入）。
- 依赖：`requirements.txt` 已加 `akshare`（免费、无需 key，但偶发反爬/限速，需联网）。

### 13.11 扩展与动态权重 / 回测的联动

- **回测可见性**：`backtest.py` 顶部 `try/except` 导入 `extensions` 导出的 `EXT_LIST_TYPES / EXT_SCORING / EXT_HIT_THRESHOLD / EXT_IC_KEYS`，并入对应注册表 → 抄底/反转/防御清单在「回测评估」下拉框直接可选，打分、达成率、单因子 IC 同口径生效。
- **动态权重**：`backtest.dynamic_weight_plan` 作用于**五策略权重**（`S1~S5`）；扩展清单是独立选股逻辑，不参与该权重计划，而是通过 `market_regime` 的"主攻/暂停"建议来调度。两者配合构成"天气系统 + 攻守清单 + 失效因子归零"的完整闭环。